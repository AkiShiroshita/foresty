% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/foresty_interaction.R
\name{foresty_interaction}
\alias{foresty_interaction}
\title{Exposure effect within each level of an effect modifier}
\usage{
foresty_interaction(
  fit,
  exposure,
  interaction,
  measure = NULL,
  exponentiate = TRUE,
  labels = NULL,
  level_labels = NULL,
  ci_level = 0.95,
  contrast = 1,
  at = NULL,
  vcov = NULL,
  cluster = NULL,
  test = c("lrt", "wald", "both"),
  table = TRUE,
  columns = NULL,
  layout = NULL,
  title = NULL,
  subtitle = NULL,
  html = FALSE
)
}
\arguments{
\item{fit}{A fitted model. If it already contains the exposure by modifier
interaction it is used as it stands; otherwise it is updated to add it.}

\item{exposure}{Name of the exposure variable, as a character string.
Naming it, as \code{exposure = c(NO2 = "no2")}, gives it that label on the
figure, which saves repeating it in \code{labels}.}

\item{interaction}{Name of the effect modifier, as a character string, and
named as \code{interaction = c(Sex = "sex")} where the figure is to call it
something other than what the column is called. It must be a factor with
at least two levels. A continuous modifier is rejected: categorize it with
\code{\link[=cut]{cut()}} first, so that the subgroups being compared are the ones you
intend.}

\item{measure}{Effect measure, one of \code{"OR"}, \code{"RR"}, \code{"HR"}, \code{"IRR"},
\code{"MD"} or \code{"Coefficient"}. The default reads it from the model: a logistic
regression gives an odds ratio, a Cox model a hazard ratio, a Poisson
model with an offset an incidence rate ratio, and a linear model a mean
difference. Ratios are exponentiated; differences are not.}

\item{exponentiate}{Whether a ratio measure is drawn as a ratio. \code{TRUE}, the
default, draws an odds ratio as an odds ratio. \code{FALSE} leaves it on the
scale the model was fitted on: the figure reports a log odds ratio, read
against zero rather than against one, and the estimates and their
intervals come back on that scale from \code{\link[generics:tidy]{tidy()}} as well. It says nothing
about a mean difference or a coefficient, which are on that scale already
and are never exponentiated.}

\item{labels}{Named character vector giving the label to draw for a
variable, as \code{c(no2 = "Nitrogen dioxide")}. Names not matched are left as
they are.}

\item{level_labels}{Named character vector giving the label to draw for a
level of the modifier, as \code{c(F = "Female", M = "Male")}.}

\item{ci_level}{Confidence level of the intervals. Defaults to \code{0.95}.}

\item{contrast}{For a continuous exposure, the increment the effect is
reported per. Defaults to \code{1}. The increment is written beside the
variable, so \code{contrast = 10} draws the row as \code{"NO2 (per 10)"}, but no
unit is invented, since the package cannot know what a column's numbers
mean; name the unit in \code{labels}, as \code{c(no2 = "NO2, ug/m3")}, and the row
reads \code{"NO2, ug/m3 (per 10)"}. It says nothing about a categorical
exposure, whose comparisons are its levels.}

\item{at}{The two values of the exposure to contrast, as \code{c(from, to)}.
Which two they were is written beside the exposure, as \code{"NO2 (20 vs 10)"},
wherever the exposure is named: every row of a figure is that same
comparison taken within another subgroup, so it is said once rather than
on each of them. An exposure entered as a spline, or in any other way that
spreads it over more than one coefficient, has no single effect to report
and is drawn only when \code{at} names the two values; any other exposure may
be given them too, \code{at} and \code{contrast} being two ways of saying the same
thing and only one of them accepted at a time. For a categorical exposure
the two values are two of its levels.}

\item{vcov}{Robust standard errors. \code{NULL}, the default, uses the model's
own. \code{"robust"} gives the heteroskedasticity-consistent sandwich estimator
(\code{HC1}), and \code{"HC0"} to \code{"HC4"} name one exactly; both come from the
\code{sandwich} package. A function is called on the fit, and a matrix is used
as it stands. For a Cox model refit with \code{robust = TRUE}, and for an \code{rms}
fit use \code{\link[rms:robcov]{rms::robcov()}}; a fit that is already robust is used as it is.}

\item{cluster}{Cluster-robust standard errors, passed to
\code{\link[sandwich:vcovCL]{sandwich::vcovCL()}}. It says which observations belong together, so it
takes a column name, a vector of one identifier per observation, or a
one-sided formula: \code{cluster = "practice_id"}, \code{cluster = data$practice_id}
or \code{cluster = ~practice_id}.}

\item{test}{How the interaction is tested: \code{"lrt"}, the default, is the
likelihood ratio test against the same model without the interaction
terms; \code{"wald"} is the joint Wald test of those terms; \code{"both"} reports
the two of them in columns of their own. See \emph{Testing the interaction}.}

\item{table}{Whether to draw the table of numbers beside the plot. Defaults
to \code{TRUE}, a forest plot being read from the numbers as much as from the
marks; \code{table = FALSE} leaves a plain figure, and \code{summary()} and \code{\link[generics:tidy]{tidy()}}
report the same numbers at the console either way.}

\item{columns}{Which columns the table carries, from \code{"estimate"}, \code{"p"},
\code{"n"}, \code{"events"}, \code{"person_time"}, \code{"interaction_p"} and, where both
tests of an interaction were asked for, \code{"interaction_p_lrt"}. The default
shows the estimate and the p-value together with whichever of the others
the models can supply. On a figure reporting a test of an interaction the
p-value of each row is left off, being easily read as the test beside it;
name it in \code{columns} to have it back.}

\item{layout}{How the figure is drawn: the name of a style, as
\code{layout = "jama"}, or a layout built by \code{\link[=foresty_layout]{foresty_layout()}} when something
about it has to be changed. The styles are \code{"classic"}, \code{"jama"},
\code{"nejm"}, \code{"lancet"}, \code{"bmj"} and \code{"revman"}.}

\item{title}{Plot title. The default names the measure, the outcome it is a
measure of, the exposure, the modifier and the fact that every subgroup
estimate comes out of one model carrying their interaction term, as
\code{"Adjusted odds ratio for asthma associated with NO2 within each level of Sex, from one model containing their interaction term"}. \code{NA} draws none,
and the journal styles draw none, a caption being where a journal puts
that.}

\item{subtitle}{Plot subtitle. \code{NULL}, the default, draws none.}

\item{html}{Whether to write the HTML report. \code{FALSE}, the default, writes
nothing, so nothing leaves the session unless it is asked for. \code{TRUE}
writes it to a file named for the variables it is about, the exposure and
the modifier joined by an underscore, so that NO2 by sex is written to
\code{no2_sex.html} in the working directory. A path writes it there instead,
as \code{html = "reports/no2_sex.html"}. The report can also be written at any
time afterwards with \code{\link[=foresty_report]{foresty_report()}}.}
}
\value{
A \code{ggplot2} object, of class \code{foresty}, carrying the estimates and
the interaction test. Print it to draw it. \code{\link[=summary]{summary()}} reports the
coefficients and the test, and \code{\link[generics:tidy]{tidy()}} returns the estimates as a data
frame.
}
\description{
Takes a model that does not yet interact the exposure with the modifier,
updates it with the interaction term, and estimates the exposure effect
separately within every level of the modifier. The estimates are drawn one
level above the other, a row to a level, and can be written to a
self-contained HTML page carrying the joint test of the interaction.
}
\details{
The point is to make an interaction readable. The p-value of an interaction
term reports only that the subgroups depart from a common effect; it does
not say which subgroup carries the effect, in which direction, or how far
apart they are, and a small p-value from a large study can accompany
subgroup estimates that are practically identical. Seeing the
subgroup-specific estimates and the test together is what settles whether an
interaction is worth reporting.

Each subgroup effect is a linear combination of the coefficients of the
single interaction model, not a separate model fitted in each subgroup: in
the reference level of the modifier it is the exposure term alone, and in
the other levels it is the exposure term plus the relevant interaction
terms. Every subgroup therefore shares one estimate of the covariate
effects, and the interaction can be tested. The combinations and their
tests are computed by \code{\link[car:linearHypothesis]{car::linearHypothesis()}}.
}
\section{Testing the interaction}{


The p-value beside the subgroups is the joint test that every exposure by
modifier coefficient is zero. It is the likelihood ratio test by default,
comparing the likelihood of the model carrying the interaction with that of
the same model without it.

\code{test = "wald"} takes the joint Wald test over the coefficients and their
covariance instead, which is what a model summary reports and what a robust
or cluster-robust variance changes. The two tests answer the same question
and usually agree; they part company where the Wald approximation is poor --
a small study, a rare outcome, a sparse subgroup, an estimate far from the
null -- and the likelihood ratio test is the more trustworthy of the two
there, which is why it is the default. \code{test = "both"} reports each in a
column of its own, which is a way of showing that the reported p-value does
not turn on which test was chosen.

The likelihood ratio test is computed from the likelihood, so it has no
robust form: it takes no account of a variance passed through \code{vcov} or
\code{cluster}. A quasi-likelihood family and a GEE have no likelihood to take it
over at all. Where it cannot be taken, or where it would not match the
intervals drawn beside it, the Wald test is reported in its place and
\code{foresty} says so; ask for one by name and you are given that one or an
error. For a linear model it is the chi-square form rather than the exact F
test, which is what the Wald test gives there.
}

\section{Adjusting the figure}{


The result is a \code{ggplot2} object, so layers, scales and themes are added to
it as usual, and \code{+} always reaches the forest:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{foresty_main(list(fit), "no2") + ggplot2::coord_cartesian(xlim = c(0.8, 2))
foresty_main(list(fit), "no2") + ggplot2::theme_minimal(base_size = 14)
}\if{html}{\out{</div>}}

\code{&} reaches every panel of a figure that has more than one, which is worth
knowing about but rarely what you want: a scale or a coordinate system
applied to the table of numbers beside the plot will spoil it. Use \code{+}.
}

\examples{
fit <- glm(asthma ~ no2 + sex + maternal_smoking + maternal_age,
           family = binomial, data = foresty_cohort)

# Sex, where the interaction is real.
by_sex <- foresty_interaction(fit, exposure = "no2", interaction = "sex")
by_sex
summary(by_sex)

# Maternal smoking, where it is not.
foresty_interaction(fit, exposure = "no2", interaction = "maternal_smoking")

# Naming the modifier labels it, and both tests of the interaction can be
# reported side by side.
foresty_interaction(fit, exposure = c(NO2 = "no2"),
                    interaction = c(Sex = "sex"), test = "both")

# The subgroup estimates, the numbers behind them and the test of the
# interaction, in the layout of a journal.
foresty_interaction(fit, exposure = "no2", interaction = "sex",
                    layout = "jama")

}
\seealso{
\code{\link[=foresty_main]{foresty_main()}}, \code{\link[=foresty_layout]{foresty_layout()}}, \code{\link[=foresty_report]{foresty_report()}}.
}
