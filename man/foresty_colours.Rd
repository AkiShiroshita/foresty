% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/colours.R
\name{foresty_colours}
\alias{foresty_colours}
\title{The qualitative palettes a figure can be coloured from}
\usage{
foresty_colours(palette = c("Dark2", "Set1", "Set2"), n = NULL, start = 1)
}
\arguments{
\item{palette}{Name of the palette: \code{"Dark2"}, the default, which is the
one a figure printed in colour is usually drawn from, \code{"Set1"} or
\code{"Set2"}.}

\item{n}{How many colours to return. \code{NULL}, the default, returns the whole
palette. More than the palette holds cycles round it, since a figure with
more categories than the palette has colours has to draw them somehow.}

\item{start}{Which colour of the palette to begin at, counting from 1. The
palette is rotated rather than trimmed, so that every colour is still
available after it.}
}
\value{
A character vector of colours.
}
\description{
The ColorBrewer qualitative palettes, as \code{RColorBrewer::brewer.pal()} gives
them, so that \code{colour} and \code{colours} in \code{\link[=foresty_layout]{foresty_layout()}} can be answered
by naming a palette and a place in it rather than by pasting a hex code.
The values are the palettes' own and are held here so that the package
needs nothing installed to draw in them.
}
\examples{
foresty_colours("Dark2")

# The third colour of Dark2, for one figure drawn in one colour.
foresty_colours("Dark2")[3]

# A palette beginning there, for a figure whose rows are coloured by their
# category.
foresty_colours("Dark2", start = 3)

}
\seealso{
\code{\link[=foresty_layout]{foresty_layout()}}, whose \code{colour} and \code{colours} arguments these
are for.
}
