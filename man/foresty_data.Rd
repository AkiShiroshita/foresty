% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/foresty_data.R
\name{foresty_data}
\alias{foresty_data}
\title{Draw a forest plot and its table from a data frame of estimates}
\usage{
foresty_data(
  data,
  estimate = NULL,
  conf.low = NULL,
  conf.high = NULL,
  label = NULL,
  group = NULL,
  n = NULL,
  events = NULL,
  person_time = NULL,
  p = NULL,
  interaction_p = NULL,
  reference = NULL,
  emphasis = NULL,
  measure = "OR",
  outcome = NULL,
  ratio = NULL,
  adjusted = FALSE,
  ci_level = 0.95,
  table = TRUE,
  columns = NULL,
  person_time_unit = NULL,
  layout = NULL,
  label_header = NULL,
  title = NULL,
  subtitle = NULL,
  xlab = NULL
)
}
\arguments{
\item{data}{The estimates, one row per row of the figure. A data frame, a
\code{tibble}, a \code{data.table}, or anything \code{\link[=as.data.frame]{as.data.frame()}} accepts.}

\item{estimate, conf.low, conf.high}{The columns holding the effect and the
two ends of its interval. \code{NULL}, the default, looks for them by name.}

\item{label}{The column holding what each row is called. \code{NULL} looks for
\code{label}, \code{term}, \code{subgroup}, \code{name} or \code{variable}, and falls back on the
row numbers.}

\item{group}{The column that blocks the rows into subgroups, or \code{NULL} for a
figure of one block.}

\item{n, events, person_time}{The columns of counts to write in the table
beside the plot, where the data carries them.}

\item{p}{The column of p-values for the rows themselves. A figure carrying a
test of the interaction leaves this column off unless \code{columns} asks for
it by name: two columns of p-values side by side are read for each other.}

\item{interaction_p}{The column of p-values for the interaction, written
once per block.}

\item{reference}{A logical column marking rows that are a definition rather
than an estimate -- the reference level of a categorical exposure -- which
are drawn without an interval and written "1.00 (reference)". \code{NULL}
treats a row whose interval is missing and whose estimate is the null
value as one.}

\item{emphasis}{A logical column marking the rows drawn for emphasis.}

\item{measure}{What the estimates are, as one of \code{"OR"}, \code{"RR"}, \code{"HR"},
\code{"IRR"}, \code{"MD"} and \code{"Coefficient"}, or as a name of your own --
\code{"Standardised mean difference"}, \code{"Prevalence ratio"} -- in which case
say with \code{ratio} which side of the null it is drawn about.}

\item{outcome}{What the measure is a measure of, named so that the axis
reads "Odds ratio for incident asthma" rather than "Odds ratio".}

\item{ratio}{Whether the estimates are ratios, drawn about 1 on a scale
where the null is 1, rather than differences drawn about 0. \code{NULL}, the
default, takes it from \code{measure}, which can only be done for a measure
named by one of the codes above; a measure described in words of your own
has to say, and it is an error not to.}

\item{adjusted}{Whether the estimates are adjusted, which is a word the axis
and the heading of the table say if so. \code{foresty} cannot tell from a
column of numbers, so it asks.}

\item{ci_level}{The confidence level the intervals were formed at, which is
what the heading of the table reports. It is not used to compute anything:
the interval is the one in the data.}

\item{table}{Whether to draw the table of numbers beside the plot.}

\item{columns}{Which columns of that table to draw, and in what order. See
\code{\link[=foresty_main]{foresty_main()}}.}

\item{person_time_unit}{The unit person-time is reported in, as
\code{person_time_unit = 1000}. Optionally named, to head the column.}

\item{layout}{The style of the figure, as a name or a \code{\link[=foresty_layout]{foresty_layout()}}.}

\item{label_header}{What to write over the column of row labels. \code{NULL}
writes "Subgroup" over a blocked figure with nothing emphasised, and
nothing over the rest.}

\item{title, subtitle, xlab}{The title over the figure, the line under it, and
the label under the plot. \code{NULL} takes the one the measure implies and
\code{NA} draws none.}
}
\value{
A \code{foresty} figure: a \code{ggplot}/\code{patchwork} object carrying its
estimates, which prints as the figure and can be added to as any \code{ggplot}
can.
}
\description{
Takes the estimates you already have -- one row per row of the figure, with
the effect and the two ends of its interval -- and draws the figure that
\code{\link[=foresty_app]{foresty_app()}} draws: the forest plot and, beside it, the table of numbers,
aligned row for row, in any of the journal styles of \code{\link[=foresty_layout]{foresty_layout()}}.
}
\details{
This is the entry point for numbers that did not come out of a model this
package can read. A meta-analysis, a table being redrawn from a paper, a
model fitted by something \code{foresty} does not support, a set of results typed
out by hand: pass the data frame, name the columns holding the estimate and
its interval, and the figure is the same figure.
}
\section{The data}{


A \code{data.frame}, a \code{tibble}, a \code{data.table} or a matrix, all of which are
read as the plain data frame the columns are taken from. One row is one row
of the figure, drawn in the order the rows are in, so sort the data the way
the figure should read.

The estimate and the two ends of its interval are the only columns the
figure cannot be drawn without. They are looked for by name when they are
not named here -- \code{estimate}, \code{conf.low} and \code{conf.high}, and the usual
alternatives (\code{lower}/\code{upper}, \code{lcl}/\code{ucl}, \code{ci_low}/\code{ci_high}) -- so a
frame that came out of \code{broom::tidy(conf.int = TRUE)} needs nothing said
about it at all.
}

\section{Subgroups}{


\code{group} names a column that blocks the rows: its values become the headings
the rows sit under, in the order they first appear. A block holding a single
row takes no heading of its own -- the row is labelled with the block
instead -- which is what makes an "Overall" row a row rather than a heading
with one line under it.

\code{emphasis} names a logical column marking the rows drawn for emphasis: a
bolder label, a diamond rather than a square, and a rule setting the block
off from the ones around it. It is how the overall estimate is set apart
from the subgroups read against it. See \code{\link[=foresty_layout]{foresty_layout()}}.

\code{interaction_p} names a column of p-values for the interaction. One test
covers the block it was taken across, so it is written once, against the
first row of the block, rather than repeated down the column.
}

\section{What is not here}{


The figure carries no model, because it was not given one. \code{summary()},
\code{\link[=foresty_report]{foresty_report()}}, \code{coef()}, \code{vcov()} and the coefficient table are
properties of a model and refuse on a figure drawn this way, naming
\code{\link[=foresty_main]{foresty_main()}} and \code{\link[=foresty_interaction]{foresty_interaction()}} as the way to have them.
\code{as.data.frame()}, \code{broom::tidy()} and \code{print()} work as they do on any
other \code{foresty} figure.
}

\examples{
subgroups <- data.frame(
  subgroup  = c("Overall", "Female", "Male", "Under 35", "35 and over"),
  block     = c("Overall", "Sex", "Sex", "Maternal age", "Maternal age"),
  overall   = c(TRUE, FALSE, FALSE, FALSE, FALSE),
  estimate  = c(1.24, 1.05, 1.48, 1.11, 1.39),
  conf.low  = c(1.08, 0.86, 1.21, 0.90, 1.14),
  conf.high = c(1.42, 1.28, 1.81, 1.37, 1.69),
  n         = c(4000, 2009, 1991, 1832, 2168),
  events    = c(802, 327, 475, 341, 461),
  p_int     = c(NA, 0.012, NA, 0.106, NA)
)

foresty_data(
  subgroups,
  label = "subgroup", group = "block", emphasis = "overall",
  interaction_p = "p_int",
  measure = "OR", outcome = "asthma", adjusted = TRUE
)

# Every combination of two categorical variables against one of them, which
# is what foresty_interaction(reference = ) draws from a model: the blocks
# are the levels of the modifier, the rows the levels of the exposure, and
# one row of the figure is the group the rest are read against.
cells <- data.frame(
  ecog      = c("0-1", ">=2", "0-1", ">=2"),
  mutation  = c("Negative", "Negative", "Positive", "Positive"),
  estimate  = c(1.00, 1.94, 0.62, 1.21),
  conf.low  = c(NA, 1.42, 0.44, 0.83),
  conf.high = c(NA, 2.65, 0.87, 1.76),
  n         = c(212, 168, 190, 145),
  events    = c(126, 131, 92, 96),
  reference = c(TRUE, FALSE, FALSE, FALSE),
  p_int     = c(0.031, NA, NA, NA)
)

foresty_data(
  cells,
  label = "ecog", group = "mutation", reference = "reference",
  interaction_p = "p_int", measure = "HR", outcome = "death",
  adjusted = TRUE, layout = "jama"
)

}
\seealso{
\code{\link[=foresty_main]{foresty_main()}} and \code{\link[=foresty_interaction]{foresty_interaction()}}, which start from a
fitted model; \code{\link[=foresty_layout]{foresty_layout()}} for the styles; \code{\link[=foresty_app]{foresty_app()}}, whose
figures this one reproduces.
}
