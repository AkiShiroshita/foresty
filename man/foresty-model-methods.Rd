% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{foresty-model-methods}
\alias{foresty-model-methods}
\alias{predict.foresty}
\alias{coef.foresty}
\alias{vcov.foresty}
\alias{formula.foresty}
\alias{nobs.foresty}
\alias{model.frame.foresty}
\title{Model methods for foresty figures}
\usage{
\method{predict}{foresty}(object, ..., model = NULL)

\method{coef}{foresty}(object, ..., model = NULL)

\method{vcov}{foresty}(object, ..., model = NULL)

\method{formula}{foresty}(x, ..., model = NULL)

\method{nobs}{foresty}(object, ..., model = NULL)

\method{model.frame}{foresty}(formula, ..., model = NULL)
}
\arguments{
\item{object, x, formula}{A \code{foresty} object. The argument is named \code{formula}
in \code{model.frame()} only because the generic names it that.}

\item{...}{Passed on to the method for the underlying fit.}

\item{model}{Which model is meant, when the figure covers several.}
}
\value{
Whatever the corresponding method for the underlying model returns.
}
\description{
These pass through to the model the figure was drawn from, so that a foresty
result can be used wherever the fit itself would have been.
}
\examples{
fit <- glm(asthma ~ no2 + sex, family = binomial, data = foresty_cohort)
x <- foresty_interaction(fit, exposure = "no2", interaction = "sex")
head(predict(x, type = "response"))
formula(x)
nobs(x)

}
