% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/colors.R
\name{foresty_colors}
\alias{foresty_colors}
\title{The qualitative palettes a figure can be colored from}
\usage{
foresty_colors(palette = c("Dark2", "Set1", "Set2"), n = NULL, start = 1)
}
\arguments{
\item{palette}{Name of the palette: \code{"Dark2"}, the default, which is the
one a figure printed in color is usually drawn from, \code{"Set1"} or
\code{"Set2"}.}

\item{n}{How many colors to return. \code{NULL}, the default, returns the whole
palette. More than the palette holds cycles round it, since a figure with
more categories than the palette has colors has to draw them somehow.}

\item{start}{Which color of the palette to begin at, counting from 1. The
palette is rotated rather than trimmed, so that every color is still
available after it.}
}
\value{
A character vector of colors.
}
\description{
The ColorBrewer qualitative palettes, as \code{RColorBrewer::brewer.pal()} gives
them, so that \code{color} and \code{colors} in \code{\link[=foresty_layout]{foresty_layout()}} can be answered
by naming a palette and a place in it rather than by pasting a hex code.
The values are the palettes' own and are held here so that the package
needs nothing installed to draw in them.
}
\examples{
foresty_colors("Dark2")

# The third color of Dark2, for one figure drawn in one color.
foresty_colors("Dark2")[3]

# A palette beginning there, for a figure whose rows are colored by their
# category.
foresty_colors("Dark2", start = 3)

}
\seealso{
\code{\link[=foresty_layout]{foresty_layout()}}, whose \code{color} and \code{colors} arguments these
are for.
}
