% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{foresty_report}
\alias{foresty_report}
\title{Write a foresty figure and its numbers to a self-contained HTML page}
\usage{
foresty_report(
  x,
  file,
  title = NULL,
  model = NULL,
  width = 10,
  height = NULL,
  open = FALSE
)
}
\arguments{
\item{x}{An object returned by \code{\link[=foresty_interaction]{foresty_interaction()}} or \code{\link[=foresty_main]{foresty_main()}}.}

\item{file}{Path to write to. A \code{.html} extension is added if missing.}

\item{title}{Page title. The default names the exposure and the modifier.}

\item{model}{Which model to report the coefficients of, when the figure
covers several. The default reports every one of them.}

\item{width, height}{Size of the embedded plot in inches. \code{height} defaults
to a size that fits the number of rows.}

\item{open}{Whether to open the file when it has been written. Defaults to
\code{FALSE}.}
}
\value{
The path written to, invisibly.
}
\description{
Writes what an interaction p-value was standing in for: the exposure effect
within each level of the modifier, the joint test of the interaction, the
ratio of the effects between levels, the whole coefficient table of the
updated model, and the figure. Everything is inlined, so the file can be
opened or sent on its own.
}
\details{
A figure drawn from several models -- \code{\link[=foresty_main]{foresty_main()}} over a list of them --
carries each of them on the page, in a section of its own headed with the
exposure it was fitted for. \code{model} cuts that down to one.
}
\examples{
fit <- glm(asthma ~ no2 + sex + maternal_age, family = binomial,
           data = foresty_cohort)
x <- foresty_interaction(fit, exposure = "no2", interaction = "sex")
foresty_report(x, file = file.path(tempdir(), "no2_by_sex.html"))

# The same page as the figure is made, under a name written from the
# variables it is about -- here no2_sex.html.
\dontrun{
foresty_interaction(fit, exposure = "no2", interaction = "sex", html = TRUE)
foresty_main(list(fit), exposure = "no2", html = TRUE)
}

}
