% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layout.R
\name{foresty_layout}
\alias{foresty_layout}
\title{Layout and style of a foresty figure}
\usage{
foresty_layout(
  style = c("classic", "jama", "nejm", "lancet", "bmj", "revman"),
  base_size = NULL,
  family = NULL,
  colour = NULL,
  colour_by = NULL,
  colours = NULL,
  theme = NULL,
  palette = NULL,
  point_shape = NULL,
  point_size = NULL,
  emphasis_shape = NULL,
  emphasis_height = NULL,
  emphasis_size = NULL,
  emphasis_face = NULL,
  emphasis_gap = NULL,
  interval_width = NULL,
  null_line = NULL,
  grid = NULL,
  axis_line = NULL,
  band = NULL,
  rules = NULL,
  separators = NULL,
  group_position = NULL,
  table_side = NULL,
  header_face = NULL,
  group_face = NULL,
  digits = NULL,
  p_format = NULL,
  decimal_mark = NULL,
  ci_separator = NULL,
  ci_brackets = NULL,
  column_gap = NULL,
  headings = NULL,
  xlim = NULL,
  arrows = NULL,
  arrows_position = NULL,
  plot_width = NULL,
  min_plot_width = NULL,
  auto_labels = NULL
)
}
\arguments{
\item{style}{Name of the style to start from, one of \code{"classic"},
\code{"jama"}, \code{"nejm"}, \code{"lancet"}, \code{"bmj"} and \code{"revman"}. A
\code{foresty_layout} object is also accepted, and is then the thing being
modified.}

\item{base_size}{Base font size in points. Everything else is drawn relative
to it.}

\item{family}{Font family, as \code{"serif"} or \code{"Times New Roman"}. \code{NULL}
leaves the device's default.}

\item{colour}{One colour for the estimates and their intervals, which is the
usual thing to change. \code{palette} changes them apart, and \code{colour_by} draws
the rows in colours of their own instead. \code{\link[=foresty_colours]{foresty_colours()}} names a
colour of a ColorBrewer palette without pasting a hex code, as
\code{colour = foresty_colours("Dark2")[3]}.}

\item{colour_by}{What the colours change with, for a figure drawn in more
than one. \code{"none"}, the default, draws the whole figure in \code{colour}.
\code{"category"} gives every category of the rows a colour of its own: the
levels of a categorical exposure -- "Yes" and "No" -- where the rows are
levels, and the subgroups of the modifier where they are subgroups, which
is what an interaction figure draws. A category keeps its colour wherever
it appears, so a combined figure reads across its blocks. \code{"row"} gives
every row a colour, whatever it is of. The reference level of a
categorical exposure is drawn hollow either way, being a definition rather
than an estimate. No legend is drawn: every row is labelled already, and a
legend repeating the labels is a second copy of them to keep in step.}

\item{colours}{The colours \code{colour_by} draws the categories in, in order,
and cycled where there are more categories than colours. The name of a
ColorBrewer palette -- \code{"Dark2"}, the default, \code{"Set1"} or \code{"Set2"} -- or
the colours themselves, as \code{c("#1B9E77", "#D95F02")}. \code{\link[=foresty_colours]{foresty_colours()}}
builds one starting from a chosen place in a palette, as
\code{colours = foresty_colours("Dark2", start = 3)}.}

\item{theme}{The \code{ggplot2} theme the plot itself is drawn on: \code{"void"}, the
default, which is the plain panel a forest plot is usually drawn as, or
the name of one of \code{ggplot2}'s own -- \code{"minimal"}, \code{"bw"}, \code{"classic"},
\code{"light"}, \code{"linedraw"}, \code{"grey"} or \code{"dark"} -- for its background, its
border and its grid. A theme object or a theme function is also accepted.
It is the plot's theme, not the figure's: the columns of numbers beside it
are a table rather than a plot and keep their own. Everything the layout
sets -- the sizes, the colours of the text, whether the axis line and the
grid are drawn -- is applied over it, so \code{grid = TRUE} and \code{theme = "bw"}
are not two answers to the same question.}

\item{palette}{Named character vector overriding single colours, as
\code{c(estimate = "#B24745", null = "grey60")}. The names are \code{estimate},
\code{border} (the outline of the marks), \code{reference} (the fill of the
reference level's hollow mark), \code{interval}, \code{null}, \code{rule}, \code{band},
\code{text}, \code{header}, \code{group} and \code{axis}.}

\item{point_shape, point_size}{Plotting symbol and its size. The default is a
filled square, as a forest plot is usually drawn with.}

\item{emphasis_shape, emphasis_height, emphasis_size, emphasis_face}{How a row
singled out for emphasis is drawn -- the overall estimate on a
\code{\link[=foresty_combine]{foresty_combine()}} figure, which a reader should be able to find without
hunting for it.

\code{emphasis_shape} is a plotting symbol, and the row is drawn as the other
rows are -- a mark on an interval -- in that symbol at \code{emphasis_size},
which may be left \code{NULL} for \code{point_size} times 1.5. The default, \code{23}, is
the filled diamond, which says the row is the summary while leaving its
interval to be read as the others are.

\code{emphasis_shape = "diamond"} draws instead the wide summary diamond a paper
draws: a filled diamond whose two side vertices sit on the confidence
limits and whose apex sits on the estimate, drawn in place of the interval
rather than on top of it. \code{emphasis_height} is how tall that diamond is, as
a fraction of the space between one row and the next, and \code{emphasis_size}
says nothing about it.

\code{emphasis_face} is the font face of the row's label, and applies either
way.}

\item{emphasis_gap}{Whether a row singled out for emphasis is set apart from
the rest by a rule of its own, drawn whether or not \code{separators} rules
between subgroups, so that the overall estimate is read as its own thing
rather than as another subgroup.}

\item{interval_width}{Line width of the confidence intervals.}

\item{null_line}{Line type of the line at the null, as \code{"dashed"} or
\code{"solid"}. \code{"none"} draws none.}

\item{grid}{Whether to draw vertical grid lines behind the estimates.}

\item{axis_line}{Whether to draw the axis line under the plot.}

\item{band}{Whether to shade alternate rows, which helps a reader carry the
eye across a wide table of numbers.}

\item{rules}{Where to rule the figure: \code{"header"} draws a line under the
column headings, \code{"full"} adds one above them and one under the last row,
and \code{"none"} draws neither.}

\item{separators}{Whether to draw a thin rule between one subgroup and the
next.}

\item{group_position}{Where the name of a subgroup goes: \code{"row"} puts it on
a row of its own above the rows it covers, and \code{"column"} puts it in a
column of its own to the left of them.}

\item{table_side}{Which side of the plot the numbers are written on,
\code{"right"} or \code{"left"}.}

\item{header_face, group_face}{Font face of the column headings and of the
subgroup names, as \code{"bold"} or \code{"plain"}.}

\item{digits}{Digits the estimates are written to.}

\item{p_format}{How p-values are written. \code{"default"} writes \code{0.032} and
\verb{<0.001}; \code{"jama"} drops the leading zero and rounds as JAMA asks, to
three places up to \code{0.01} and two above it.}

\item{decimal_mark}{Decimal point, \code{"."} unless a journal asks otherwise.}

\item{ci_separator}{What goes between the confidence limits. The default
picks \code{"-"} when every number on the figure is positive and \code{" to "} when
one is not, a hyphen between negative numbers being unreadable.}

\item{ci_brackets}{The pair of brackets the interval is written in, as
\code{c("[", "]")}.}

\item{column_gap}{Space between one column of numbers and the next, in ems,
an em being about two digits wide. Lower it to draw the numbers tighter
and leave the plot more of the figure.}

\item{headings}{Named character vector renaming the column headings, as
\code{c(n = "No. of patients")}. The names are \code{estimate}, \code{label}, \code{p}, \code{n},
\code{events}, \code{person_time}, \code{interaction_p}, and \code{interaction_p_wald} and
\code{interaction_p_lrt} for a figure reporting both tests of the interaction.
The estimate's heading is
written from the model, naming the measure and the confidence level; the
outcome is named on the axis under the plot rather than twice over.
\code{label} heads the column of row labels, and defaults to \code{"Exposure"}
in \code{\link[=foresty_main]{foresty_main()}}, the modifier's name in \code{\link[=foresty_interaction]{foresty_interaction()}} and
\code{"Subgroup"} in \code{\link[=foresty_combine]{foresty_combine()}}, which leaves it unheaded when the
figure carries an overall estimate as well as subgroups.}

\item{xlim}{Limits of the plot, as \code{c(0.5, 4)}. Intervals running past them
are drawn with an arrow at the end, which is what keeps one wide interval
from flattening the rest of the figure.}

\item{arrows}{Two labels for the directions of the effect, as
\code{c("Favours treatment", "Favours control")}, drawn under the plot with an
arrow apiece. \code{NULL} draws none.}

\item{arrows_position}{Whether those labels go at the \code{"bottom"} of the plot
or at the \code{"top"}.}

\item{plot_width}{How much of the figure the plot itself takes. \code{NULL}, the
default, gives it whatever the columns of text leave, so that widening the
figure widens the plot and nothing else. A fraction, as \code{0.6}, gives it
that share of the figure whatever the figure is drawn at, and the columns
of text share the rest. A number of centimetres, or a \code{\link[grid:unit]{grid::unit()}},
fixes it exactly.}

\item{min_plot_width}{The least of the figure the plot is allowed to be left
with, as a fraction. What the columns of text leave depends on the width
the figure is drawn at, which is not known while it is being built, so a
wide table -- a survival model carrying N, events and person-time -- can
leave the plot a centimetre and squash the axis under it into a row of
overprinted numbers. This is the floor: where the columns of text would
leave the plot less than this share of the width it is being drawn at, the
plot is given that share and the columns of text share the rest in
proportion to what they hold. \code{0} turns the floor off and lets the plot
have whatever is left, however little that is. It has no effect where
\code{plot_width} says outright how wide the plot is.}

\item{auto_labels}{Whether \code{\link[=foresty_interaction]{foresty_interaction()}} writes its own title and
subtitle. The journal styles leave them off, since that text belongs in
the caption; a title passed by hand is always drawn.}
}
\value{
An object of class \code{foresty_layout}.
}
\description{
Every drawing decision the figure makes is held in one object: the colours,
the marks, the rules between the rows, how the numbers beside the plot are
written, and where the columns sit. Pass the name of a style to
\code{\link[=foresty_main]{foresty_main()}} or \code{\link[=foresty_interaction]{foresty_interaction()}} as \code{layout = "jama"}, or build
one here and change any part of it.
}
\details{
The styles are the ones a paper is usually asked for, read off the forest
plots those journals print and the layouts of the \code{meta} package, which
\code{foresty} follows here. They are approximations of a house style, not the
style sheet itself: a journal will still ask for its own fonts and sizes,
and every element below can be overridden.

\describe{
\item{\code{"classic"}}{The default. Black marks on white, a dashed line at the
null, a rule under the column headings and thin rules between subgroups.}
\item{\code{"jama"}}{Numbers to the left of the plot, rules above and below the
whole block, navy squares, a solid line at the null, p-values without
their leading zero (\code{.03}, \verb{<.001}), and "No." and "P value" as
headings. The title and subtitle that \code{\link[=foresty_interaction]{foresty_interaction()}} writes for
itself are left off, the caption being where a journal puts them.}
\item{\code{"nejm"}}{Numbers to the right, blue squares, en dashes between the
confidence limits, "P value" as the heading.}
\item{\code{"lancet"}}{As \code{"nejm"}, in the Lancet's blue, with the middle dot
for a decimal point, so that 1.17 is written with one, and en dashes.}
\item{\code{"bmj"}}{The BMJ's violet, grey rules, and "to" between the
confidence limits.}
\item{\code{"revman"}}{Cochrane's RevMan: blue squares and intervals written
\code{1.15 [0.93, 1.42]}.}
}
}
\examples{
fit <- glm(asthma ~ no2 + sex + maternal_smoking + maternal_age,
           family = binomial, data = foresty_cohort)

foresty_interaction(fit, "no2", "sex", table = TRUE, layout = "jama")

# A style, changed where it needs to be.
foresty_interaction(
  fit, "no2", "sex", table = TRUE,
  layout = foresty_layout("jama", colour = "#B24745", base_size = 11)
)

# Trimming a wide interval rather than letting it flatten the figure.
fit_urban <- glm(asthma ~ urbanicity + sex + maternal_age,
                 family = binomial, data = foresty_cohort)
foresty_main(list(fit_urban), "urbanicity", table = TRUE,
             layout = foresty_layout("classic", xlim = c(0.8, 2.5),
                                     arrows = c("Lower risk", "Higher risk")))

}
\seealso{
\code{\link[=foresty_main]{foresty_main()}}, \code{\link[=foresty_interaction]{foresty_interaction()}}.
}
