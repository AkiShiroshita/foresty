% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{foresty-tidiers}
\alias{foresty-tidiers}
\alias{tidy.foresty}
\alias{glance.foresty}
\alias{as.data.frame.foresty}
\title{Turn a foresty figure into a data frame}
\usage{
\method{tidy}{foresty}(
  x,
  what = c("estimates", "coefficients"),
  conf.int = TRUE,
  model = NULL,
  ...
)

\method{glance}{foresty}(x, ...)

\method{as.data.frame}{foresty}(x, row.names = NULL, optional = FALSE, ...)
}
\arguments{
\item{x}{A \code{foresty} object.}

\item{what}{\code{"estimates"}, the default, returns the rows drawn on the
figure. \code{"coefficients"} returns the whole coefficient table of the model
instead, on the scale it was fitted on.}

\item{conf.int}{Whether to include the confidence interval. Defaults to
\code{TRUE}, an interval being the point of a forest plot.}

\item{model}{Which model to take the coefficients from, when the figure
covers several.}

\item{...}{Ignored.}

\item{row.names, optional}{Ignored, present for consistency with the generic.}
}
\value{
A data frame.
}
\description{
\code{tidy()} returns the estimates the figure was drawn from, one row apiece, in
the columns \code{broom} uses and in the order it puts them, so the result reads
beside a tidied model and drops into the same pipelines. \code{glance()} returns
one row describing the fit.
}
\details{
\code{as.data.frame()} returns everything the figure carries instead, the display
labels included, which is what to use when the estimates are going back into
a plot rather than into a table. It is the one of the three that needs
nothing installed.

\code{tidy()} and \code{glance()} are \code{broom}'s generics, and \code{foresty} registers its
methods on them rather than carrying \code{broom} itself: install it if you want
them, and call them as \code{broom::tidy(x)} or after \code{library(broom)}.
}
\examples{
fit <- glm(asthma ~ no2 + sex + maternal_age, family = binomial,
           data = foresty_cohort)
x <- foresty_interaction(fit, exposure = "no2", interaction = "sex")
as.data.frame(x)

if (requireNamespace("broom", quietly = TRUE)) {
  broom::tidy(x)
  broom::glance(x)
}

}
